[package]
name = "wargames"
version = "0.1.0"
edition = "2021"

[dependencies]
console_error_panic_hook = "0.1.7"
dotenv = "0.15.0"
leptos = { version = "0.7.0", features = ["csr"] }
leptos_router = "0.7.0"
