#[derive(Debug)]
pub enum PoliticalStatus {
    Russian,
    Teutonic,
}

#[derive(Debug)]
pub enum Way {
    Waterway,
    Trackway,
}

#[derive(Debug)]
pub enum Locale {
    Castle,
    Fort,
    TradeRoute,
    Bishopric,
    City,
    Novgorod,
    Town,
    Region,
}

#[derive(Debug)]
pub enum Territory {
    Livonia,
    Estonia,
    Rus,
}